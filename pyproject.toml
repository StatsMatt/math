[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "math-tools"
version = "0.1.0"
description = "Personal math tools"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["math_tools*"]
exclude = ["scratch*"]